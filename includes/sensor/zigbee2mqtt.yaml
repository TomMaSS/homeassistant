# zigbee2mqtt bridge state sensor (legacy MQTT approach)
- sensor:
    - name: "Zigbee Bridge State"
      unique_id: zigbee2mqtt_bridge_state_mqtt
      state: "{{ states('sensor.zigbee2mqtt_bridge_state') | default('unknown') }}"
      icon: mdi:router-wireless

# Alternative: Direct MQTT sensor (if addon doesn't expose entity)
# Uncomment below if needed:
# - platform: mqtt
#   name: Bridge state
#   state_topic: "zigbee2mqtt/bridge/state"
#   icon: mdi:router-wireless
